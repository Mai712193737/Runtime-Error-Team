#include <bits/stdc++.h>
using namespace std;
int main (){}



/*X. Grid Rectangle Perimeter Calculation
X. Grid Rectangle Perimeter Calculation
You are given an n×m
 grid containing '.' and '#' symbols.

Each '.' (dot) represents a blank area.
Each '#' (hash) represents a filled area.
Filled areas contribute to the creation of exactly one rectangle, where each '#' symbol forms the four sides of a rectangle.

Both the width and height of the formed rectangle will be at least 3
.

Your task is to calculate the perimeter of the rectangles formed by the filled areas in the given grid.

The perimeter of a rectangle is the total length of its outer boundary, which is the sum of the lengths of all four sides.

Input
The first line contains two integers, n
 and m
 (2≤n,m≤100
). The number of rows and columns of the grid respectively.

The next n
 lines contain m
 characters each (either a hash or a dot).

Output
Print the perimeter of the rectangle formed by hashes in the grid.*/